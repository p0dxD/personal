apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: blog-postgres-pvc
  namespace: podoi
  labels:
    app: blog-postgres
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 2Gi
